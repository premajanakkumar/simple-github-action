name: Run shell command
on: push

jobs:
    run-shell-command:
        runs-on: ubuntu-latest
        steps:
                - name: Echo string
                  run: echo "Hello World"
                